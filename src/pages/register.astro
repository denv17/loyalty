---
import RegisterForm from '@/components/RegisterForm.astro'
import Layout from '@/layouts/Layout.astro'
import { Image } from 'astro:assets'
import image from '@/images/bicho.jpg'
---

<Layout title="Register">
  <section class="relative min-h-dvh flex items-center pt-32 pb-16">
    <div class="absolute inset-0 bg-[url(/images/bg.svg)] grayscale">
      <div class="absolute inset-0 bg-gradient-to-r from-white/80 to-white">
      </div>
    </div>
    <div
      class="container relative grid grid-cols-1 lg:grid-cols-5 gap-8 lg:gap-16 lg:items-center"
    >
      <div class="lg:col-span-2 lg:order-1 flex flex-col gap-8">
        <div class="flex flex-col gap-6">
          <h1 class="text-4xl xl:text-5xl">Un café gratis te espera ☕</h1>
          <p class="text-lg">
            Obtén <strong>la bebida de tu preferencia</strong> como regalo al acumular
            <strong>5</strong> sellos.
          </p>
        </div>

        <RegisterForm />
      </div>
      <div class="lg:col-span-3">
        <Image
          class="aspect-[6/7] object-cover w-full lg:max-w-2xl border border-black"
          src={image}
          alt="Bicho"
        />
      </div>
    </div>
  </section>
</Layout>
